#include<bits/stdc++.h>
using namespace std;
int main(){
   map<char,char>m;
   for(int i=0;i<26;i++){
    cin>>m.first();
   }
      for(int i=0;i<26;i++){
    cin>>m.second();
   }
   string s,s1;
   cin>>s;
   for(int i=0;i<n;i++){
    if(m.first[i]==s[i])[
        s1.push(m.second());
    ]
   }

    return 0;
}