#include <bits/stdc++.h>
using namespace std;
#define nl "\n"
#define ll long long
#define int long long
using pii = pair<int, int>;
void magic(){
    float n,m;
    cin>>n>>m;
    float bc=2*sqrt((n/2)*(n/2)+m*m);
    float ac=sqrt(bc*bc-n*n);
    cout<<ac<<nl;

 
}
int32_t main(){
    ios::sync_with_stdio(false);
    cin.tie(0);

  
        magic();
    
    return 0;
}