#include <bits/stdc++.h>
using namespace std;
#define nl "\n"
#define ll long long
#define int long long
using pii = pair<int, int>;
void magic(){
    vector<char>c(20),x(26,0);
    

    cin>>s;
    for(int i=0;i<n;i++){
        cin>>c[i];
        
    }
    map<char,int>m;
    for(int i=0;i<20;i++){
        m[v[i]]++;
    }
    for(int i=n-1;i>=0;i--){
        s[i]
    }
 
}
int32_t main(){
    ios::sync_with_stdio(false);
    cin.tie(0);

    int t;
    cin >> t;
    while(t--){
        magic();
    }
    return 0;
}